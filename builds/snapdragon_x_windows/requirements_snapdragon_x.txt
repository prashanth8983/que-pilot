# Requirements for Snapdragon X Windows ARM64 build
# Core dependencies
python-pptx>=0.6.21
opencv-python>=4.8.0
pytesseract>=0.3.10
Pillow>=10.0.0
numpy>=1.24.0
psutil>=5.9.0

# Build tools
pyinstaller>=5.13.0

# Windows ARM64 specific
pywin32-ctypes>=0.2.0

# Note: Platform-specific dependencies are excluded:
# - pyobjc-framework-Cocoa (macOS only)
# - pyobjc-framework-Quartz (macOS only) 
# - pywin32 (Windows only, handled by pywin32-ctypes for cross-compilation)
